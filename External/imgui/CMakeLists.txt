file(GLOB IMGUI_CODES "${CMAKE_CURRENT_SOURCE_DIR}/*.h" "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp" "${CMAKE_CURRENT_SOURCE_DIR}/backends/*.h" "${CMAKE_CURRENT_SOURCE_DIR}/backends/*.cpp")

set(TARGET_NAME imgui)

add_custom_target(${TARGET_NAME} SOURCES ${IMGUI_CODES})